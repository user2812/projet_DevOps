pipeline {
    agent any

    environment {
        DOCKER_IMAGE = "fatimag-docker/django-app"
        DOCKER_TAG = "latest"
    }

    stages {
        stage('Cloner le dépôt') {
            steps {
                git branch: "main", url: 'https://github.com/user2812/projet_DevOps.git'
            }
        }

        stage('Construire l\'image Docker') {
            steps {
                bat "docker build -t $DOCKER_IMAGE:$DOCKER_TAG ."
            }
        }

        stage('Tester l\'application') {
            steps {
                bat "docker run --rm $DOCKER_IMAGE:$DOCKER_TAG python manage.py test"
            }
        }

        stage('Exécuter le conteneur') {
            steps {
                bat """
                    docker stop django-container || echo "Aucun conteneur à arrêter"
                    docker rm django-container || echo "Aucun conteneur à supprimer"
                    docker run -d --name django-container -p 8000:8000 $DOCKER_IMAGE:$DOCKER_TAG
                """
            }
        }

        stage('Notifier par email') {
            steps {
                emailext subject: "Déploiement Jenkins terminé",
                        body: "Le déploiement de l'application Django est terminé avec succès.",
                        to: 'gassama761@gmail.com'
            }
        }
    }
}